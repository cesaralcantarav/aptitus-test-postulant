logger:
  verbose: True

endpoint:
  postulant:
    dev: "https://dev{senv}.aptitus.com/registro"
    pre: "https://pre{senv}.aptitus.com/registro"
    prod: "https://aptitus.com/registro"

s3:
  input:
    bucket: qa.{env}
    key:  test/aptitus/{env}/postulant/data/input

  output:
    bucket: qa.{env}
    key:  test/aptitus/{env}/postulant/data/output
